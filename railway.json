{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "ls -la /app && python -c 'import sys; print(sys.path); import main' && uvicorn main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
